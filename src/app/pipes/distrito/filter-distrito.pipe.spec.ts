import { FilterDistritoPipe } from './filter-distrito.pipe';

describe('FilterDistritoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDistritoPipe();
    expect(pipe).toBeTruthy();
  });
});
